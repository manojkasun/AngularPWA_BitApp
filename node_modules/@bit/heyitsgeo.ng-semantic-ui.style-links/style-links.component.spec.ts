import { ComponentFixture, TestBed } from '@angular/core/testing';

import { StyleLinksComponent } from './style-links.component';

describe('StyleLinksComponent', () => {
  let component: StyleLinksComponent;
  let fixture: ComponentFixture<StyleLinksComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ StyleLinksComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(StyleLinksComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
